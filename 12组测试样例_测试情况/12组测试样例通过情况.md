# 📊 OSComp 内核glibc测试用例状态追踪表
## 注意musl 全部产生死锁，下面全是glibc样例测试


| 序号 | 测试项 (Test Suite) | 类别 (Category) | 当前状态 (Status) | 关键依赖 & 备注 (Notes) |
| :---: | :--- | :--- | :--- | :--- |
| 1 | **Basic** | 基础功能 | ⚠️ **killed** | `brk`, `open`, `read`, `mmap` 等核心系统调用已验证复活。 |
| 2 | **Busybox** | 基础环境 | ✅ **通过** | 依赖 `fork`, `exec` 和文件系统。内存修复后高概率通过。 |
| 3 | **Lua** | 运行时环境 | ✅ **通过**  | 解释器强依赖动态堆内存 (`brk`/`mmap`)，之前必挂，现在理论上能跑通。 |
| 4 | **Libctest** | C 库功能 | ⚠️ **killed**| 验证标准库 (musl/glibc) 兼容性，只要 Basic 过了它通常也能过。 |
| 5 | **Libcbench** | 内存性能 | ⚠️ **killed**| 纯内存操作测试 (`memcpy`, `memset`)，应能正常运行。 |
| 6 | **Unixbench** | 综合跑分 | ⚠️ **killed** | 只要不崩就是胜利。`Process Creation` 分数可能受 `cow` 机制影响。 |
| 7 | **Iozone** | 文件 IO | ⚠️ **killed** | 考验 VFS 和页缓存 (Page Cache) 的健壮性。 |
| 8 | **Lmbench** | 系统延迟 | ✅ **通过**  | **注意**：因 `nanosleep` 被强制 `return 0`，延迟类测试结果可能显示为 0us 或无效。 |
| 9 | **Iperf** | 网络吞吐 |  ❌**error - unable to connect to server** | 取决于网络协议栈 (TCP/IP) 和虚拟网卡驱动是否就绪。 |
| 10 | **Netperf** | 网络延迟 | ⚠️ **killed** | 同上，强依赖网络子系统。 |
| 11 | **Cyclictest** | 实时性 | ❌ **失败，并且产生死锁** | 专门测试时钟抖动。因 Timer 有 Bug 且我们短路了睡眠，此项大概率挂科或数据极差。 |
| 12 | **LTP** | 稳定性 | ❌ **失败，并且产生死锁**  | “大魔王”，包含数千个极端边界测试。通常作为最终压力测试。 |

---

### 📝 图例说明

* ✅ **通过 (Passed)**: 运行无报错，输出符合预期。
* ⏳ **待验证 (Pending)**: 尚未运行，但根据当前修复情况，理论上应该能运行。
* ⚠️ **数据异常 (Warning)**: 能跑通，但由于修改了 `nanosleep` 逻辑，输出的性能数据可能不准确。
* ❌ **预计失败 (Expected Fail)**: 预期会报错、


### **Lmbench测试说明**

/ext2/glibc # ./busybox sh ./lmbench_testcode.sh

#### OS COMP TEST GROUP START lmbench-glibc ####

latency measurements

Simple syscall: 26.9110 microseconds

Simple read: 47.3005 microseconds

Simple write: 47.7484 microseconds

Simple stat: 653.5471 microseconds

Simple fstat: 221.8222 microseconds

Simple open/close: 881.4068 microseconds

Select on 100 fd's: 1153.1463 microseconds

Signal handler installation: 135.4378 microseconds

Signal handler overhead: 512.4316 microseconds

Protection fault: 35.5629 microseconds

Pipe latency: 2690.8059 microseconds

Process fork+exit: 80740.3571 microseconds

Process fork+execve: 80794.0769 microseconds

Process fork+/bin/sh -c: 182167.1429 microseconds

File /var/tmp/XXX write bandwidth:9874 KB/sec

Pagefaults on /var/tmp/XXX: 90.2868 microseconds

0.524288 4036

file system latency

0k	497	477	671

1k	393	341	469

4k	377	355	535

10k	362	328	459

Bandwidth measurements

Pipe bandwidth: 23.76 MB/sec

0.524288 118.72

0.524288 97.21

0.524288 8040.03

0.524288 37.67

context switch overhead

"size=32k ovr=184.33

2 1317.35

4 1261.29

8 1265.45

16 1244.75

24 1286.84

32 1262.64

64 1323.36

96 1269.65

#### OS COMP TEST GROUP END lmbench-glibc ####
